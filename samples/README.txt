sample1.m
Starting from 0 (lowerbound) exclusive, display all numbers with an increment of 5 up to to 50 (upperbound) inclusive.

sample2.m
With inputs (a, b, c), display the result of a^2 - (b * c).

Note: This program assumes that all inputs and outputs are all natural numbers.